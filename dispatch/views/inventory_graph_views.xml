<?xml version="1.0"?>
<odoo>
    <!-- <data>
        <record id="view_inherit_inventory_graph" model="ir.ui.view">
            <field name="name">Inherit Inventory Graph</field>
            <field name="model">stock.picking.batch</field>
            <field name="arch" type="xml">
                <graph string="Inventory Metrics" type="bar">
                    <field name="weight" type="measure" string="Weight"/>
                    <field name="volume" type="measure" string="Volume"/>
                    <field name="transfers" type="measure" string="Volume"/>
                    <field name="lines" type="measure" string="Volume"/>
                </graph>
            </field>
        </record>

        <record id="action_inherit_inventory_graph" model="ir.actions.act_window">
            <field name="name">Inventory Metrics</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">stock.picking.batch</field>
            <field name="view_mode">graph</field>
            <field name="view_id" ref="view_inherit_inventory_graph"/>
        </record>

        <menuitem id="menu_inherit_inventory_graph" name="Inventory Metrics" parent="base.menu_reporting" action="action_inherit_inventory_graph"/>
    </data> -->
</odoo>
